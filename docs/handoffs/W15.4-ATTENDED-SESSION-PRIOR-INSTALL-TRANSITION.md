# W15.4 — Attended Session Prior-Install Transition Repair

## Assignment

Issue: #83  
Worker lineage: W15  
Branch: `agent/attended-session-prior-install-transition`  
Branch created from canonical main: `645fd7bed35816b0e64e4b01f552c95bc4fcc917`

At startup, independently resolve and report the exact current branch head. Do not assume this handoff commit or base SHA is still the branch head.

## Read first

- `AGENTS.md`
- `docs/AGENT-OPERATING-PATTERNS.md`
- `docs/LOCAL-OPERATOR-COMMANDS.md`
- `docs/CLEAN-ROOM-TESTING.md`
- `docs/PLAYER-DISABLE-UNINSTALL.md`
- `docs/PARALLEL-AGENT-WORKFLOW.md`
- `docs/INTEGRATION-ORCHESTRATOR.md`
- issue #83
- `tools/Start-BiologyAttendedSession.ps1`
- `tools/BiologyAttendedSession.Core.ps1`
- `tools/Bootstrap-BiologyManagedPostTransitionCandidate.ps1`
- `tools/Bootstrap-BiologyPostTransitionCandidate.ps1`
- `tools/Build-BiologyPackage.ps1`
- `src/uninstaller/BiologyUninstallCore.cs`
- `src/uninstaller/BiologyUninstallerProgram.cs`
- `tests/Test-PlayerUninstaller.ps1`
- `tests/BiologyUninstallCoreTests.cs`
- `tests/Test-OwnedSessionTool.ps1`

## Attended evidence that triggered this follow-up

The owner ran the new one-command attended session against exact canonical source:

`645fd7bed35816b0e64e4b01f552c95bc4fcc917`

with transition evidence:

`w11-transition-cleanup-20260915-224207-4df9bb80`

Returned bundle:

`Biology-Operator-Evidence-attended-645fd7bed358-20260916-205430-24718827.zip`

Session result:

- `FAIL-CLOSED`
- phase `PREPARATION-FAILED-BEFORE-READY`
- managed preparation result `FAIL-CLOSED`
- game mutation started `False`
- installed candidate remained the previous exact source `f643bbc1c50a69d223c2cf54e9fc7f68215e33fd`
- current game version `2.31`

The managed candidate pre-install residue check correctly refused to overlay a new candidate because the prior Biology install remained:

- `mods/Biology`
- `r6/scripts/CyberpunkRealism`
- `biology`
- `Uninstall Biology.exe`
- `BIOLOGY-VERSION.txt`
- `INSTALL.txt`
- `UNINSTALL.txt`
- `SHA256SUMS.txt`

Immediately before the session, the player-facing uninstaller itself refused before mutation with:

`Biology-owned deletion is not allowed at this path: BiologyReleaseInstall.Core.ps1`

## Proven source-contract mismatch

`tools/Build-BiologyPackage.ps1` intentionally emits both root-side installer files as Biology-owned payload:

- `Install Biology.ps1`
- `BiologyReleaseInstall.Core.ps1`

But `BiologyUninstallPlanner.BiologyOwnedRootFiles` currently allows only:

- `INSTALL.txt`
- `UNINSTALL.txt`
- `BIOLOGY-VERSION.txt`
- `SHA256SUMS.txt`
- `Uninstall Biology.exe`

That means the current uninstaller can reject a valid receipt produced by the current package builder.

## Required outcome

Repair the narrow release lifecycle so both of these are true:

1. A valid current package receipt is accepted by the player uninstaller for every package-root Biology-owned file that the builder intentionally emits, while arbitrary/forged root-level Biology ownership remains fail-closed.
2. The canonical owner-facing attended contract remains one command/session. A previously installed exact Biology candidate must not force the owner into a separate manual uninstall, separate probe, or manual deletion step before the next attended candidate can be prepared.

The implementation may reuse the schema-2 receipt and existing hash-bounded uninstall/install primitives. Prefer one source of truth for ownership and transition safety rather than creating a second discrete deletion model inside the session tool.

## Acceptance requirements

Automated coverage must prove at minimum:

- every intentional Biology-owned root file emitted by `Build-BiologyPackage.ps1` is accepted by uninstall planning;
- an arbitrary additional root file marked `biology-owned` is still rejected;
- shared `redscript` and `cybercmd` payload remains preserve-only;
- changed Biology-owned files remain preserved/fail-closed rather than blindly removed;
- a one-command attended session starting with a prior schema-2 Biology candidate can safely transition internally to the requested exact candidate and reach the normal `READY TO LAUNCH CYBERPUNK` boundary without asking the owner to pre-uninstall;
- failure before READY still yields the same one evidence bundle and no ambiguous game mutation;
- `END -> one evidence ZIP -> SENT -> SESSION ENDED CLEANLY` semantics remain intact;
- exact-source/zero-local-repo behavior remains intact.

Run the relevant focused tests plus the full cloud-safe CI suite. Open/update a PR to `main` and return READY-PARENT with exact branch head and validation results.

## Scope boundaries

Do not merge your own PR.  
Do not ask the owner to install/play the worker branch.  
Do not ask the owner to reinstall Cyberpunk.  
Do not require a separate pre-test uninstall command.  
Do not broaden into Biology UI/detail behavior, E3 presentation, body runtime, physiology/combat/scanner, REDscript startup redesign, sentinel redesign, or dependency redesign.  
Do not weaken path/hash/receipt collision safety to make updates easier.
