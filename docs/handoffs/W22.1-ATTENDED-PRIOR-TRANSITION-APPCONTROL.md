# W22.1 — T006 pre-launch replacement transition / Windows Application Control

Parent: **P02**  
Issue: **#141**  
Source attended test: **T006 / #140**  
Branch: `agent/attended-prior-transition-appcontrol`  
Branch base at creation: `d87326b9b11f972a5974770eebbb284724ebfe1d`

## Startup

At startup independently resolve and report:
- exact current worker branch head;
- exact current canonical main;
- issue #141 and latest comments;
- T006 issue #140 and latest comments;
- branch diff against current main.

Do not assume the handoff/base SHA is still current.

## Evidence packet

Owner returned:
`Biology-Operator-Evidence-attended-d87326b9b11f-20260919-203433-2cc177c5.zip`

Bundle SHA-256:
`37D3FE94F1E33EE182AF17A836262BED4B79A3786CB679A6A62F2E5C34F66257`

If the packet is attached directly in the worker chat, inspect it yourself.

## Proven state

The integrated T006 candidate exact-compiled successfully against the owner's installed Cyberpunk 2077 2.31 game before the preparation failure.

Evidence:
- redscript 0.5.31 verified;
- cybercmd 0.0.13 verified;
- 65 source scripts compiled successfully;
- source-only owned runtime profile PASS;
- deployable runtime profile PASS;
- package and artifact policy PASS;
- candidate artifact built:
  `biology-integrated-20260920-013440-d87326b9b11f.zip`
- artifact SHA-256:
  `CF6C9F9AB43759BD00A3D5C77BC91164507C6F27EFFC2D42C91ED16769A914B0`

This closes W21's parent exact-2.31 compile integration gate.

T006 gameplay did not start.

## Broken boundary

The existing T005 schema-2 candidate was recognized:
- sourceRevision `a5818db6596e335824d75f596fc8204cc419de4f`
- buildId `biology-integrated-20260920-002342-a5818db6596e`

Collision preflight for the new target passed.

The replacement path then compiled:
`BiologyPriorInstallTransition.exe`

into:
`%LOCALAPPDATA%\Temp\biology-prior-install-transition-...\`

Compilation passed, but Windows rejected process creation with Win32 error 4551:

`An Application Control policy has blocked this file.`

The failure is before mutation:
- Game mutation started: False
- Prior Biology transition attempted: True
- Prior Biology transition passed: False
- Prior Biology transition mutation started: False
- Target install mutation started: False
- Installed receipt exact revision verified: False
- REDmod deployment passed: False

The installed T005 candidate was preserved.

## Current implementation seam

Read:
- `tools/Bootstrap-BiologyPostTransitionCandidate.ps1`
- `tools/Build-BiologyUninstaller.ps1`
- `src/uninstaller/BiologyPriorInstallTransitionProgram.cs`
- `src/uninstaller/BiologyUninstallCore.cs`
- uninstaller/recovery tests and W14/W15 release evidence docs.

The current candidate-prep path:
1. creates a temp helper directory;
2. uses built-in csc through `Build-BiologyUninstaller.ps1 -TransitionOutputPath`;
3. launches the newly compiled `BiologyPriorInstallTransition.exe`;
4. expects its report;
5. only then installs the target candidate.

The receipt/hash/path-bounded semantics are valuable and must remain authoritative.

## Goal

Make exact prior schema-2 candidate replacement work on owner systems where Windows Application Control blocks newly generated helper executables.

Do this without weakening system security and without broadening mutation.

Preferred properties:
- no requirement to launch an ad-hoc unsigned/generated EXE from Temp;
- reuse the existing uninstaller core semantics rather than creating a looser second implementation;
- all preflight remains before mutation;
- shared dependencies remain preserve-only;
- changed/untracked Biology-owned files remain fail-closed;
- target collision preflight still happens before prior retirement;
- failure-durable reporting remains one-command compatible.

## Forbidden workarounds

Do not:
- tell owner to disable Smart App Control / WDAC / AppLocker;
- require elevation/admin solely to bypass policy;
- move/delete installed files manually;
- ignore the current receipt;
- treat a blocked helper as permission to proceed;
- duplicate only part of the uninstaller validation in an unsafe script.

## Validation

Before READY-PARENT:
- direct tests of the new transition execution path;
- regression that no generated transition EXE is launched;
- existing uninstaller/release/recovery tests remain green;
- full Biology CI green;
- current main reconciled;
- PR open/clean;
- exact final head reported;
- explain whether the existing failed T006 evidence is sufficient for the exact-compile gate (it is expected to be) and provide the minimal T006 rerun boundary.

Do not request gameplay on the worker branch.
Do not merge your own PR.
P02 owns integration and T006 resume.

---

## W22.1 implementation record

Worker PR: **#142 — W22.1: avoid generated transition executable under Application Control**

Implementation boundary:

- `tools/Bootstrap-BiologyPostTransitionCandidate.ps1` no longer creates, compiles, or launches a Temp `BiologyPriorInstallTransition.exe`.
- `tools/Build-BiologyUninstaller.ps1` no longer exposes `-TransitionOutputPath`; the player-facing `Uninstall Biology.exe` build remains intact.
- `tools/Invoke-BiologyPriorInstallTransition.ps1` is the attended transition host. It runs under the already-used `pwsh` execution path and writes the same failure-durable transition report consumed by the candidate bootstrap.
- `tools/BiologyPriorInstallTransition.Core.ps1` builds the receipt-bounded transition plan from the installed schema-2 receipt.
- Actual file removal is delegated to the pre-existing `Invoke-BiologyFailedInstallRecoveryPlan` exact-removal executor in `tools/BiologyFailedInstallRecovery.Core.ps1`; W22.1 does not introduce a second deletion primitive.

The W22.1 planner mirrors the player-uninstaller authority before invoking that executor:

- exact schema-2 Biology product/uninstall contract;
- `playableRuntimeIncluded=true`;
- `officialPackageRoot=mods/Biology`;
- Biology-owned vs `generic-dependency-shared` policy split;
- explicit Biology-owned roots: `mods/Biology/`, `r6/scripts/CyberpunkRealism/`, and `biology/`;
- explicit supported Biology-owned game-root filenames;
- SHA-256 validation of every Biology-owned receipt entry;
- duplicate, malformed, unsafe, reparse-point, arbitrary-root, untracked-file, and untracked-directory refusal;
- full owned-namespace inventory revalidation immediately before mutation;
- receipt identity revalidation immediately before mutation;
- receipt ordered after all other Biology-owned files so a bounded execution failure does not discard ownership evidence early;
- shared dependencies are preserve-only even when their bytes have changed since the Biology receipt was written.

The attended ordering remains:

1. exact target compile/build;
2. target collision preflight against the current install;
3. receipt-bounded prior-candidate transition;
4. Biology-specific residue verification;
5. second target collision preflight;
6. exact target install;
7. receipt verification;
8. official REDmod deployment.

No WDAC / Smart App Control / AppLocker policy change, elevation workaround, manual deletion, hash bypass, or receipt bypass is introduced.

## Regression coverage

`tests/Test-PlayerUninstaller.ps1` now drives the prior-candidate fixtures through the PowerShell transition host rather than through a generated transition executable.

Coverage includes:

- valid schema-2 prior candidate transitions successfully;
- exact Biology-owned files and the prior receipt are removed;
- shared dependencies remain present;
- a shared dependency changed after receipt creation remains preserved and does not block Biology replacement;
- an arbitrary forged Biology-owned game-root filename fails closed before mutation;
- an untracked empty directory in a Biology-owned namespace fails closed before mutation;
- changed Biology-owned content fails closed before mutation;
- bootstrap/build scripts contain no `BiologyPriorInstallTransition.exe` / `TransitionOutputPath` dependency;
- player-uninstaller planner/executor safety tests remain active;
- release/recovery tests remain part of the unchanged full CI suite.

A full push CI and pull-request CI were green on predecessor head
`a822c96d15b24ec1ab97917eef986f108392726e` after the transition-host fix.
The final tip additionally adds the changed-shared-dependency regression and is required to finish the same full CI before READY-PARENT.

## T006 evidence / resume boundary

The durable T006 record in #140 is sufficient for the **integrated exact-compile gate**:

- canonical integrated source `d87326b9b11f972a5974770eebbb284724ebfe1d`;
- installed Cyberpunk 2077 2.31;
- all 65 REDscript sources compiled successfully;
- target package/artifact built successfully;
- target collision preflight passed.

The failure happened only when Windows refused to start the generated transition helper, before any prior-candidate or target-install mutation. The T005 schema-2 candidate therefore remains the valid starting installed state.

After P02 integrates #142, resume **the same T006 ID** at the candidate-preparation / pre-launch boundary. No separate replay of the already-proven d873 exact-compile evidence is required as a parent acceptance gate. The preparation command may naturally exact-compile/rebuild the integrated candidate again as part of its normal one-command workflow; once preparation succeeds, continue into the original T006 attended gameplay checks.

Do not test the worker branch directly and do not merge #142 from this worker lane.
