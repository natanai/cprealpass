# W23.1 — Overnight T007 authority, E3 toggle, and scanner-nameplate stabilization

Parent: **P02**  
Issue: **#151**  
Branch: `agent/t007-overnight-stabilization`  
Branch base: `d2a4c8f1c166bbfd0227451cbf32e3793f10ea72`  
Source live test: **T007 / #148**

## Mission

Work autonomously overnight and return one parent-ready integrated repair for the three live T007 failures:

1. authoritative Biology body progression/combat injuries still do not advance;
2. the relocated E3 preference row is visible but does not toggle;
3. scanner context still shows Biology ambient `NC RESIDENT` simultaneously with native detailed police identity `BEAT COP`.

This is not a redesign. Preserve the substantial live-good work already integrated.

## Startup / source of truth

Before changing code:

1. resolve exact current local branch head;
2. fetch `origin`;
3. resolve exact `origin/main`;
4. read issue #151 and T007/#148;
5. read:
   - `docs/test-runs/TEST-LEDGER.md`
   - latest T006 durable record
   - W20.2/W21.2/W21.3 handoffs
   - parent #40/#41 latest comments;
6. inspect diff between your branch and current main;
7. reconcile current main if it moved, without discarding this handoff.

Do not trust stale conversation summaries over repo state.

## Direct T007 evidence

Exact tested source:
`d2a4c8f1c166bbfd0227451cbf32e3793f10ea72`

Evidence packet:
`Biology-Operator-Evidence-attended-d2a4c8f1c166-20260920-014102-63374e83.zip`

Evidence SHA-256:
`E25A441BD4BD61DB31874E91483101E3D021ED72C65471743CB7B3FBC5F964EA`

Operational boundary:
- managed preparation PASS;
- exact CP2077 2.31 compile PASS;
- candidate installed with exact receipt;
- listener STARTED-AND-EXITED;
- fresh REDscript compile after READY;
- observed gameplay about 209.199 s;
- no matching crash/hang evidence.

T007 screenshots supplied to P02:

### Initial LEGS detail
Diagnostic:
`T007 AUTH | ticks 0 progressed 0 allowed false dt 0.000000h rate 0.000000x | body 0.000000h | combat 0 | view 6 detail-LEGS body=0.000000h tissue=0.000000 | queue 0 fault false`

Visible:
- L/R NO CONDITION
- leg function 100.0%
- bone integrity 100.0%
- E3 preference row is now correctly placed and fully visible
- row reads `E3 HUD + NAMEPLATES   ON`

### Later Biology state after ordinary play/combat
Diagnostic visibly reaches:
- `ticks 23`
- `progressed 0`
- `allowed false`
- `dt 0.000000h`
- `rate 0.000000x`
- `body 0.000000h`

Thus:
- the one-second tick loop is alive;
- body-time progression never becomes allowed;
- no authoritative elapsed body time accumulates.

### Skin/Wounds after heavy native Health loss
Diagnostic shows:
`combat 6 commit-body-gate r=0 m=0 shapes=1 v=0.000000`

and:
- tissue burden remains 0;
- queue remains 0;
- fault remains false;
- no Biology wound appears despite substantial native Health loss.

This is extremely important: combat wrappers are not completely absent. The route reaches a body-gate rejection.

### Scanner/nameplate
Native scanner detailed identity is visible as:
`BEAT COP`

while Biology's ambient red text simultaneously shows:
`NC RESIDENT`

The scanner results panel also says `BEAT COP`.

Correct authority:
native discovered/scanner identity must own scanner context.
Biology ambient fallback is presentation-only and may appear only when native context does not already own a more specific visible identity.

### E3 preference
The row is visually good and local to Biology detail, but clicking it does not change `ON` to `OFF`.

## Highest-priority root-cause lane: body authority

Start here.

Current source:

`src/redscript/CyberpunkRealism/BodyRuntime.reds`

`NativeStateAllowed(allowMenu)` currently requires:
- `this.running`;
- `CRInjuryEffectsBridge.Allowed(player, true)`;
- and, when `allowMenu == false`, not being in the menu.

Current source:

`src/redscript/CyberpunkRealism/InjuryEffectsNative.reds`

`CRInjuryEffectsBridge.Allowed(actor, enabled)` rejects if:
- disabled;
- actor undefined;
- replacer;
- dead;
- defeated;
- not attached;
- or scene system reports actor in a scene.

T007 proves:
- `running` is far enough along for ticks;
- the body input queue is healthy;
- every observed body tick is still `allowed false`;
- combat eventually reaches `commit-body-gate`.

Working hypothesis:
the shared native-state/actor progression gate is incorrectly rejecting the normal player in ordinary gameplay.

Do **not** simply remove the gate.

Prove which exact predicate is false on the live 2.31 player, and determine whether the same predicate should even be applied to the local player.

Use:
- current repo source;
- installed CP2077 2.31 script/resource evidence;
- local reference mods where useful;
- WolvenKit/read-only inspection;
- existing tests and architecture docs.

Owner machine paths:
- game: `C:\Games\Steam\steamapps\common\Cyberpunk 2077`
- private references: `C:\Games\Cyberpunk-ReferenceMods`
- reusable reference tooling: `C:\Games\Cyberpunk-ReferenceMods\_tooling`

Reference mods must remain read-only and must never become runtime dependencies.

Desired result:
- normal gameplay body ticks are allowed and advance authoritative time;
- menu/pause/scene states that genuinely should suspend progression remain suspended;
- combat can accept real player injury during ordinary gameplay;
- no progression while invalid/replacer/dead/unsupported state;
- no duplicate body authority.

## Combat lane

Continue from the T007 evidence, not from T005 guesses.

Current flow:
`DamageSystem.ProcessOneShotProtection`
-> `CRNativeWoundBridge.Prepare`
-> native damage/resource loss
-> `DamageSystem.SendDamageEvents`
-> `CRNativeWoundBridge.Commit`
-> `CRBodyRuntime.RecordInjury`
-> `CRBodyInputs.Drain`
-> authoritative saved body
-> Biology projection.

T007's latest diagnostic is `commit-body-gate`.

Determine why `CanAcceptCombatInjury()` is false at Commit and whether fixing the shared body gate also fixes this route.

Do not:
- poll Health and synthesize injury;
- invent a second wound state;
- bypass native resource loss;
- remove one-shot/boss/protection semantics;
- accept nonphysical damage as physical tissue injury.

Actual accepted native physical Health loss remains mandatory for player wound commit.

If the shared body-gate repair exposes a new downstream rejection, preserve/add bounded diagnostics and fix only with evidence.

## E3 preference lane

Files:
- `src/redscript/CyberpunkRealism/BiologyPreferencesNative.reds`
- `src/redscript/CyberpunkRealism/RealpassSettings.reds`
- `src/redscript/CyberpunkRealism/BiologyCyberwareShell.reds`

The placement fix is KEEP:
- local Biology detail host;
- PRESENTATION subsection;
- 680x48 row;
- no fullscreen-root positioning.

The click currently does not change the visible ON state.

Prove whether failure is:

A. pointer callback is not firing / guard rejects the event;
B. `CRBiologyInDetail()` is false unexpectedly;
C. `evt.GetCurrentTarget()` does not equal the row because child widgets own hit targeting;
D. action is not named `click` in this native context;
E. `CRRealpassSettings.Get(game)` is undefined, so Toggle computes a change but Set silently no-ops;
F. persisted setting changes but the row/HUD does not refresh.

Pay special attention to this suspicious behavior in `RealpassSettings.reds`:
when the settings ScriptableSystem is undefined, `SetE3FirstPersonHudVisuals` returns true without storing anything, while `UseE3FirstPersonHudVisuals` defaults to true.

Do not silently mask a missing settings authority.

Desired result:
- click/tap reliably toggles ON/OFF;
- saved ScriptableSystem is the one authority;
- UI immediately refreshes;
- E3 first-person/nameplate presentation follows the preference;
- state survives menu reopen and save lifecycle;
- no Mod Settings/external dependency.

Keep children/layout local; do not regress placement.

## Nameplate/scanner lane

Files:
- `src/redscript/CyberpunkRealism/NameplatesNative.reds`
- `src/redscript/CyberpunkRealism/E3NameplatesNative.reds`

Current behavior:
- ambient fallback resolves to `puppet.GetDisplayName()` when native `NPCNextToTheCrosshair.name` is empty;
- E3 code writes that resolved text into native `m_nameTextMain`;
- E3 code also keeps native nameplate display geometry visible;
- scanner context can simultaneously show scanner-specific `BEAT COP` and the ambient red `NC RESIDENT`.

Do not write fallback identity into native knowledge data. That previous bug is already fixed and must remain fixed.

Now solve the presentation conflict:
- identify which widget/controller owns the scanner's detailed projected identity;
- identify how the native nameplate lifecycle distinguishes scanner/quickhack context from ordinary ambient nameplate context;
- in scanner/detailed native context, native detailed identity wins and Biology ambient fallback should not be simultaneously forced visible;
- outside scanner, ordinary ambient civilian/police names may still use the public display-name fallback if native rules allow;
- preserve hidden-name/alternative-name/quest-target/nameplate-disabled rules;
- preserve modern scanner/quickhack UI.

Do not use guessed screen-space offsets or duplicate identity chrome.

## Live-good behavior that must not regress

KEEP all of these:

- Biology root tab and native anatomy shell;
- W17 authored Biology detail host;
- W19 drill-down/Back behavior: no stock Cyberware flash;
- deliberate Biology <-> Cyberware switching;
- ordinary Cyberware behavior;
- E3 preference placement/local subsection;
- broader E3 quest/hotkey/weapon presentation;
- modern scanner UI;
- no old corner reticle artifact;
- W22 Application Control-safe candidate replacement;
- REDmod-first/self-contained packaging;
- no Project E3 runtime dependency;
- no Mod Settings dependency;
- no RED4ext/ArchiveXL dependency.

## Local-machine permissions / safety

You are on the owner's PC and may use local tools more effectively than cloud workers.

Allowed:
- inspect repo and git history;
- fetch GitHub;
- inspect installed CP2077 files read-only;
- inspect `C:\Games\Cyberpunk-ReferenceMods` read-only;
- use WolvenKit/reference tooling read-only;
- run repo tests;
- run exact offline REDscript compile against installed game;
- build disposable candidate packages outside the game;
- add bounded tests/docs/diagnostics;
- push your branch and open/update PR.

Do **not**:
- launch Cyberpunk unattended;
- alter saves;
- install reference mods;
- mutate the installed game just to inspect it;
- disable Windows security;
- delete owner files;
- merge your own PR;
- force-push shared branches;
- work directly on main.

If an existing build/test command writes to the installed game, do not use it overnight. Prefer the repository's offline/disposable compile/build paths.

## Development method

Work in this order:

1. reproduce/understand the body gate statically and with local current-game evidence;
2. fix the body progression gate with the narrowest semantically correct rule;
3. re-evaluate combat path under the repaired gate;
4. fix E3 toggle authority/event routing;
5. fix scanner vs ambient nameplate visibility/authority;
6. add focused regression tests for all three;
7. run all affected component tests frequently;
8. run full Biology CI/test suite locally;
9. exact-compile the full integrated runtime against installed CP2077 2.31;
10. run packaging/artifact policy checks in disposable output only;
11. reconcile latest `origin/main`;
12. rerun tests + exact compile after reconciliation;
13. push final branch and open/update PR;
14. write a durable handoff.

Do not spend the night adding broad features. Fix the three live T007 failures and strengthen proof.

## Required tests / proof

At minimum add/update tests proving:

### Body authority
- normal local-player gameplay can be allowed to progress under the intended native-state contract;
- menu/invalid lifecycle remains disallowed where appropriate;
- the relevant player-specific allowed predicate is not accidentally inherited from NPC-only semantics;
- body tick progression still uses one authority;
- body save state remains persistent;
- diagnostics stay attended-only/transient.

### Combat
- body gate permits ordinary valid player injury route after the fix;
- no wound without accepted physical native Health loss;
- no synthetic Health polling;
- structural failure remains fail-closed.

### Preference
- local row lifecycle remains;
- no fullscreen TopRight regression;
- event path can actually mutate the saved preference authority;
- missing settings authority cannot silently pretend a toggle succeeded;
- ON/OFF refresh follows real saved state.

### Nameplate
- native scanner/detailed identity outranks ambient fallback;
- ambient fallback cannot remain forcibly visible in scanner context when native detailed identity owns the presentation;
- no fallback data is written back into native identity/knowledge;
- civilians outside scanner remain supported.

## Exact compile

Before READY-PARENT, exact-compile the complete integrated runtime against the installed Cyberpunk 2077 2.31 base.

Record:
- command used;
- compiler/tool versions;
- source count;
- PASS/FAIL;
- exact branch head.

Do not claim exact compile unless it actually ran against the installed current game.

## Deliverables

Leave all of the following before stopping:

1. branch:
   `agent/t007-overnight-stabilization`

2. issue:
   #151 updated with findings

3. clean PR against current main

4. full test/CI results

5. exact current-2.31 compile evidence

6. durable handoff:
   `docs/handoffs/W23.1-T007-OVERNIGHT-STABILIZATION.md`

7. handoff must state:
   - exact final head;
   - current main used/reconciled;
   - root cause of body `allowed false`;
   - whether body-time progression is now statically/provably repaired;
   - whether combat gets past `commit-body-gate` and what remains live-only;
   - exact E3 toggle root cause and repair;
   - exact scanner/nameplate root cause and repair;
   - tests run;
   - exact compile result;
   - what the next attended test must prove;
   - any residual uncertainty.

## Definition of READY-PARENT

READY-PARENT only when:
- branch is reconciled with current main;
- no uncommitted changes;
- PR is open and clean;
- full tests green;
- exact 2.31 compile green;
- no known static defect remains in the three targeted lanes;
- handoff is complete;
- remaining work is genuinely live-only acceptance rather than an obvious source bug.

Do not merge the PR yourself.
