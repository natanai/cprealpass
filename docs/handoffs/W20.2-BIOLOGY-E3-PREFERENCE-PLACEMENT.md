# W20.2 — T006 Biology E3 preference placement follow-up

Parent: **P02**  
Issue: **#144**  
Parent presentation issue: **#40**  
Branch: `agent/biology-e3-preference-placement-followup`  
Branch base: `fbce426fc3305f67f29ec083bfe8c9f644c2572d`  
Source test: **T006 / #140**

Read #144 first and treat its body as the complete work specification.

At startup independently resolve:
- exact worker branch head;
- canonical main;
- #144 + latest comments;
- #40 + latest comments;
- current `BiologyPreferencesNative.reds`;
- branch diff against main.

T006 direct evidence:
- exact source `fbce426fc3305f67f29ec083bfe8c9f644c2572d`;
- preference screenshot SHA256 `29C40BFDAEBA7D4EEAEB251EFBDE303ACADA5FFCE45869ABA971129B62BF1918`.

Current free-floating TopRight root text is live-proven clipped/unusable.
Move the preference into a stable Biology-owned/native layout region with a usable mouse/controller hit target. Keep save-backed CRRealpassSettings authority; do not add Mod Settings or other dependencies.

Preserve W19's live-good navigation and current native menu chrome.

Do not ask owner to test the worker branch.
Do not merge your own PR.
P02 owns integration and T007.


## Worker implementation

Startup was independently re-resolved through the GitHub connector:

- initial worker head: `4802b1086187e9291fe20a72fdef308574f309e6`;
- canonical main after P02's T006 evidence merge: `89a071a4f3190076b5c868a01eb6661b8d6e9a21`;
- initial branch state: 1 ahead / 4 behind, diverged at `fbce426fc3305f67f29ec083bfe8c9f644c2572d`;
- branch was reconciled onto current main before implementation.

### Direct T006 evidence independently inspected

Owner-supplied returned evidence bundle:

- evidence ID: `attended-fbce426fc330-20260919-213403-2b48215e`;
- ZIP SHA-256: `AB6D135F2E86D87818BACA38868ECD5FD63C8159F6768979424DE00C004EE76C`;
- result: PASS for managed preparation/session evidence;
- exact tested source: `fbce426fc3305f67f29ec083bfe8c9f644c2572d`;
- Cyberpunk 2077 2.31 / REDmod 2.31;
- package build exact-compiled all 65 Biology scripts before launch.

The owner also supplied the live screenshots directly in this worker conversation.
The full LEGS screenshot independently hashes to the durable T006 binding
`F0CBDCC21B039E7CC02F9F01337A30C7473E9DC388572C33620C15CDF8D54C46`.
The supplied crop visibly shows only the clipped `E3 HU...` fragment at the extreme
right edge, consistent with #144's live finding.

### Layout decision

W17.1 / #107 remains the structural authority:

```text
wrapper
└─ Inventory                         RipperdocInventoryController lifecycle root
   ├─ cyberwareContainer             authored stock selected-content container
   └─ CRBiologyNativeContent         Biology sibling using copied live placement
```

The preference therefore does **not** get another screen-root offset. It is
progressive-disclosure UI inside `CRBiologyNativeContent` at deliberate body-system
detail depth.

This is intentional: it keeps the setting readily reachable from Biology while
removing permanent chrome from the overview/top shell and from ordinary Cyberware.

### Source changes

`BiologyPreferencesNative.reds`:

- removes fullscreen-root lookup/reparenting;
- removes `TopRight` anchoring and the T006 fixed top/right margins;
- creates a `PRESENTATION` subsection under the supplied Biology local host;
- shows `E3 HUD + NAMEPLATES` plus a distinct `ON` / `OFF` value;
- uses a 680x48 interactive `inkCanvas` row rather than making a small text glyph the
  entire hit target;
- adds hover affordance to the row;
- reads and toggles only through `CRRealpassSettings`;
- accepts interaction only while Biology owns detail depth;
- exposes a bounded reset method for screen teardown.

`BiologyCyberwareShell.reds`:

- mounts the preference only into `crBiologyNativeContent`;
- refreshes it when the authored detail host is valid/visible;
- clears preference widget references during `RipperDocGameController` teardown;
- does not alter W19's stock-content suppression / Back ordering.

No Mod Settings, ArchiveXL, RED4ext, Codeware, or Project E3 runtime dependency is added.

### Regression coverage

Added `tests/Test-BiologyE3PreferencePlacement.ps1` and wired it into
`tests/Run-CI.ps1`.

The coverage fails if the preference returns to:

- `GetRootCompoundWidget`;
- `inkEAnchor.TopRight`;
- the old fixed right-edge margin strategy;
- a text-only tiny hit target;
- a second settings authority;
- an external settings/runtime dependency.

It also reasserts W17's `Inventory -> cyberwareContainer` authored seam and that the
preference parent is detail-lifecycle bounded.

`Test-SettingsRuntimeSurface.ps1` now separately requires local Biology content
mounting and rejects fullscreen-root/TopRight preference placement.

### Exact current-game compile boundary

A fresh exact Cyberpunk 2.31 compile of this **worker head** is not available from the
cloud worker because the proprietary installed game data/final.redscripts are not
present here.

Do not reinterpret the earlier T006 compile as a compile of W20.2. It proves the
integrated pre-W20.2 source and native 2.31 baseline. P02's normal attended candidate
builder remains the exact-current-game compile gate before T007 READY.

### T007 live acceptance

P02's integrated T007 must prove visually/interactively:

1. entering a supported Biology detail shows the entire `PRESENTATION` subsection;
2. `E3 HUD + NAMEPLATES` and current `ON` / `OFF` are fully legible;
3. the row does not overlap title/summary/metrics/context, anatomy, selector, top hub,
   level/street cred, currency, or screen-edge decoration;
4. mouse activation works anywhere on the intended row hit target;
5. controller activation works through the normal menu interaction path;
6. toggling actually changes the canonical E3 saved preference and the value display;
7. detail -> Back removes the subsection with the Biology detail surface;
8. re-entering detail recreates/refreshes the same preference cleanly;
9. repeated detail -> Back cycles preserve W19's live-good no-grid-flash behavior;
10. Biology -> Cyberware at overview leaves ordinary Cyberware behavior and chrome
    unchanged;
11. returning to Biology restores the preference only at deliberate detail depth.

P02 owns merge/integration and T007. W20.2 must not ask the owner to test this worker
branch directly.
