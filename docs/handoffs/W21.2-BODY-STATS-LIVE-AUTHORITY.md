# W21.2 — T006 Biology authoritative stats follow-up

Parent: **P02**  
Issue: **#143**  
Parent runtime issue: **#41**  
Branch: `agent/body-stats-live-authority-followup`  
Current canonical main at reconciliation: `89a071a4f3190076b5c868a01eb6661b8d6e9a21`  
Source attended test: **T006 / #140**

## Status

**READY-PARENT for review/integration, with the normal parent-owned installed-game exact-2.31 compile still required before T007.**

The worker branch is reconciled with current main and remains zero commits behind it. Do not merge this worker PR here; P02 owns integration and T007.

## Direct T006 evidence inspected

Attached packet:
`Biology-Operator-Evidence-attended-fbce426fc330-20260919-213403-2b48215e.zip`

Verified:
- exact ZIP SHA-256: `AB6D135F2E86D87818BACA38868ECD5FD63C8159F6768979424DE00C004EE76C`;
- exact tested source: `fbce426fc3305f67f29ec083bfe8c9f644c2572d`;
- evidence ID: `attended-fbce426fc330-20260919-213403-2b48215e`;
- Cyberpunk 2077: 2.31;
- managed candidate installed with exact receipt;
- listener: STARTED-AND-EXITED, 352.172 seconds;
- fresh REDscript compile after READY;
- no crash artifacts / matching Windows Application crash-hang events.

The packet contains build/startup/session evidence, not per-hit or per-tick authority telemetry.

T006 screenshot/report:
- owner reported the Biology stat surface appeared inert;
- LEGS showed healthy/no-condition values at 100%;
- W19.1 drill-down/Back transition was live-good and is preserved.

## What T006 actually proves

The LEGS capture is not itself evidence that the ordinary body clock failed. Healthy leg function/bone integrity should remain 100% until a localized leg injury commits.

Likewise, a short healthy session can produce real needs/metabolism movement that was hidden by presentation rounding:
- body model baseline loses water/energy continuously while awake;
- session/detail presentation used `RoundF(...)` to whole percentages;
- values such as ~99.8% therefore rendered as `100%`.

The source already reads detail from the one authoritative `CRBodyRuntime` snapshot/meters. There is no evidence justifying a second UI body authority or native-Health polling.

## Repair / observability

### 1. Preserve visible sub-percent authoritative movement

`BiologySessionPresentation.reds` and the legacy detail projection now render percentage text to one decimal place instead of whole-percent rounding.

This changes display precision only. The underlying authoritative Float values, thresholds, body model, save state, and damage model are unchanged.

### 2. Make T007 distinguish the real runtime boundary

The existing `CRBodyTestPolicy.Diagnostics()` gate remains canonical-false and is still opened only in staged candidates.

The managed attended bootstrap now passes `-Diagnostics` to `Build-BiologyPackage.ps1`. Ordinary package/release invocation remains diagnostics-off by default.

Transient, bounded diagnostics were added to `CRBodyRuntime`:
- tick callback count;
- count of observations with positive body-time delta;
- latest native-state-allowed result;
- latest observed body-time delta/rate;
- latest combat routing stage;
- latest presentation readback, body elapsed hours, and tissue burden.

They are:
- non-persistent;
- in-memory only;
- bounded counters;
- no external watcher/log/file/timer;
- no second body state;
- no gameplay mutation.

For attended diagnostic candidates, Biology overview/detail surfaces the status string so the owner can return a screenshot without external tooling.

### 3. Continue W21.1 combat routing without guessing

Combat model semantics are unchanged. The bridge now records bounded diagnostic stages around the existing path, including:
- `prepare-body-gate`;
- `prepare-enter`;
- `prepare-no-physical`;
- `prepare-contact-missing`;
- `prepare-contact-rejected`;
- `prepare-profile-rejected`;
- `prepare-anatomy-rejected`;
- `prepare-ready`;
- `commit-body-gate`;
- `commit-mismatch`;
- `commit-no-health-loss`;
- `commit-no-injury`;
- `commit-ready`;
- `record-rejected` / `record-input-rejected` / `record-accepted`.

A no-plan Commit does **not** overwrite the more specific Prepare rejection, so the owner-facing status retains the first useful boundary.

No armor/shape veto was relaxed from T006 alone. No generic wound was synthesized.

## Regression coverage / CI

A first intermediate CI run (#2166, head `5e7022596dd133f5308cae471ed4643bbc92d490`) failed because the REDscript-to-C# wound fixture did not translate the new REDscript `String` token and two static contract tests still expected the pre-instrumentation source spelling. Those were test-harness/contract regressions, not gameplay fixes.

They were corrected on-branch. Current final pre-handoff implementation head:
`2c38e7adb1d4c4b2b64704033a3b0ee1bec44c36`

Biology CI #2170 / run `35486513982`: **PASS**
- 94 body-runtime authority checks;
- 104 body authority/persistence checks;
- 75 shared condition/provenance architecture checks;
- 24 body-model invariant checks;
- 31 ordered body-input checks;
- 20 body presentation/migration checks across 128 migration combinations;
- 23 body forecast checks;
- 23 sleep/body-interaction checks;
- 43 injury/body integration checks;
- 76 wound-pipeline checks and 270 parameter scenarios;
- 40 NPC shared-clock/body-progression checks;
- 84 cloud-safe owned-path Biology checks total;
- uninstaller/source package/artifact policy all passed.

## Installed-game exact compile boundary

The T006 packet proves the immediately preceding integrated source compiled successfully against the installed CP2077 2.31 environment.

This worker environment does not have the owner's proprietary installed-game script/cache inputs, so it cannot truthfully re-run the exact 2.31 compiler for this new head. The changes introduce project-owned methods/fields/control flow and no new native method/type seam, but **P02 must retain the normal exact-2.31 managed candidate build as a pre-T007 integration gate**.

Do not represent cloud CI as a substitute for that exact compile.

## What T007 must prove

Use the integrated managed attended candidate and return the normal evidence packet plus screenshots.

1. **Ordinary body progression**
   - enter Biology System Replacement/whole-body detail;
   - record the T007 AUTH line;
   - play outside menus long enough for at least one body minute/tick;
   - reopen Biology.
   - Expected evidence: tick count increases, `progressed` becomes nonzero, `allowed true` occurs, body elapsed hours increase, and hydration/nutrition can visibly move below 100.0%.

2. **Combat injury**
   - take ordinary hostile gunfire that visibly lowers native Health;
   - reopen the relevant Biology body region and capture T007 AUTH.
   - If injury still does not appear, the last combat stage now identifies the first broken boundary without guessing.
   - If `record-accepted`, the authoritative injury/detail state must change region-specifically.

3. **Projection/readback**
   - `view` count and last presentation status must show the same authoritative body elapsed hours/tissue burden seen after state change.
   - No UI-local substitute values.

4. **Persistence continuity (#41)**
   Once a real changed body/injury state exists:
   - close/reopen Biology;
   - WAIT;
   - close/reopen;
   - save/reload;
   - reopen;
   - SLEEP;
   - reopen.
   The same authoritative state must continue rather than reinitialize.

5. Preserve already-live-good W19.1 detail/Back behavior and native damage authority.

P02 owns integration, exact installed-game compile, and T007.
