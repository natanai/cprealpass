# W04.2 — Body runtime persistence / authority follow-up

Parent: P01.2
Issue: #41
Base at creation: `65d0b48e805528f601ceb839cb7d1e9326cf06ac`

## Current attended boundary

On integrated candidate `205578b11d818f474dc74a873e6d6ea5a1e1accd` / CP2077 2.31:
- the old `[ BIOLOGY ERROR ] BODY RUNTIME SYSTEM MISSING` failure is gone;
- Biology reports `BODY STABLE`;
- selected anatomy targeting is correct;
- selected-system detail values are visibly present (LEGS showed left/right leg condition and left/right bone integrity at 100%).

This means W04.2 must NOT assume the current problem is missing data or stale REDscript startup. W13 startup activation is already accepted.

## Goal

Close the remaining #41 authority/persistence boundary before the next integrated attended milestone:
- prove the displayed detail values are read from the authoritative Biology body runtime rather than disconnected/static view state;
- prove source lifecycle for menu close/reopen, save/load, and time progression;
- preserve visible failure semantics if runtime authority is unavailable;
- add narrow diagnostics/regression coverage that lets the next one-command attended session distinguish authority/persistence failures without a second probe chain.

## Required approach

Audit first. Do not rewrite the body runtime merely because #41 remains open.

If the current implementation already satisfies the source/runtime lifecycle contract, add only the minimal regression/instrumentation needed for attended acceptance and return that finding to parent.

If a concrete lifecycle or authority gap is proven in source, repair only that exact gap and document the evidence.

## Preserve / do not own

- W02.3 owns Biology detail layout/presentation; do not redesign the body screen.
- W03.3 owns E3 HUD/nameplates.
- W15.5 owns attended-listener mechanics.
- Do not reopen REDscript/cybercmd/REDmod startup architecture without new direct evidence.
- Do not broaden into physiology/combat/scanner redesign.
- Do not invent placeholder/fake healthy values.

## Parent attended acceptance target

The integrated one-command session should be able to verify:
- detail values present from authoritative runtime;
- menu close/reopen retains authority;
- save/reload retains authority;
- wait/sleep/time progression does not sever authority;
- a genuine missing-runtime condition remains visibly failed rather than silently returning STABLE.

Do not merge your own PR.
Do not ask the owner to install/play the worker branch.
Do not ask the owner to reinstall Cyberpunk.