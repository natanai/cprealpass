# Biology — active REDmod completion roadmap

This ledger follows owner-authorized **#152**, not the former worker handoff sequence. Current GitHub issues/PRs and `docs/THREAD-LEDGER.md` identify coordination state. See root `ROADMAP.md`, `AGREED-GOALS.md`, `docs/PRODUCT-COMPLETION-MATRIX.md` and `docs/evidence/AUTONOMOUS-COMPLETION-2026-09-20.md` for current implementation and executed evidence.

## Product and activation

The official `mods/Biology` marker is the only whole-mod activation authority. Supplemental Biology scripts may compile while launcher mods are OFF; all native entry points must remain inert or restore only Biology-owned presentation. The 62 wrappers and 25 additive event/callback entry points are inventoried in `manifest/activation-hook-audit.json`. Owned ScriptableSystem lifecycle rules are recorded in the completion evidence.

The native body runtime remains the only persistent player physiology authority. The historical BODY RUNTIME SYSTEM MISSING finding and the later T007 progression/body-gate failures have source repairs. Body overview, detail and Back remain native-shell-driven. Civilian ambient identity yields to native identity and to the modern scanner/quickhack owner. E3 preference changes use one save-backed Boolean and immediate UI refresh.

## Local completion gates

Finish the source/model/contract audit, exact-compile all production sources, build one release, inspect its inventory and licenses, exercise install/update/reinstall/hard uninstall/reinstall through the shipped binaries, deploy official REDmod, verify startup without loading a save, and reconcile final CI/source/receipt hashes. The final candidate must remain installed. No external distribution is authorized.

The shared redscript/cybercmd dependencies remain preserved on hard uninstall. The package has no Mod Settings / ArchiveXL / RED4ext consumer. Uninstall never clears shared roots recursively or modifies saves. Native UI/save/gameplay acceptance remains distinct from automated source/package/lifecycle evidence; irreducible live observations are reported honestly under the owner directive.

## Historical evidence, not current instructions

The former attended follow-ups #39 (Biology UI), #40 (E3 presentation), #41 (body runtime), and #44 (disable/uninstall) are reconciled through #152. Their historical evidence remains intact; source fixes and local release checks are recorded separately from native gameplay observations.

W11 / PR #65 and issue #64 recorded SAFE-TO-APPLY for the pre-W10 retired framework transition, with redscript preserved. The historical Mod Settings / ArchiveXL / RED4ext footprint must not return. W15 repository evidence under `docs/operator-evidence/` supported removal of only empty Biology-owned roots when the old failed-install ZIP was absent. That missing-ZIP recovery boundary remains available for historical recovery, not as the normal release installer.

The current installer is transactional and receipt-bounded. A full Cyberpunk reinstall is not the default response to an ownership conflict, and old merged branches are not current implementation lanes.
