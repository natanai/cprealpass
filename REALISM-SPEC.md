# Retired implementation specification

Status: **retired from active guidance**  
Retired: **2026-09-15**

This path is retained only as a compatibility pointer for old links. The former `realpass` implementation specification became stale as the product was renamed **Biology**, the REDmod-first package architecture was proven in-game, and subsystem-specific architecture moved into focused canonical documents.

Do **not** use an older version of this file from Git history to determine current product intent, dependency status, packaging, runtime ownership, settings, or test procedure.

Use instead:

1. `AGREED-GOALS.md` — canonical current product intent.
2. `AGENTS.md` — mandatory development/evidence rules.
3. `ROADMAP.md` — current work.
4. `docs/BIOLOGY-REDMOD-MIGRATION.md` — package/runtime routing architecture.
5. focused architecture documents such as `docs/BIOLOGY-UI.md`, `docs/E3-PRESENTATION.md`, `docs/SETTINGS-ARCHITECTURE.md`, `docs/RELEASE-ARCHITECTURE.md`, and `docs/PATCH-RESILIENCE.md`.
6. current GitHub issues/PRs and `docs/test-runs/` for implementation status and attended evidence.

Git history preserves the retired specification when historical archaeology is genuinely needed.
