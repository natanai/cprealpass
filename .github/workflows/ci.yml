name: realpass CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: realpass-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  offline-validation:
    name: Offline models, contracts, and safe package
    runs-on: windows-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show revision
        shell: pwsh
        run: |
          Write-Host "Revision: $env:GITHUB_SHA"
          Write-Host "Ref: $env:GITHUB_REF"
          pwsh --version

      - name: Run cloud-safe realpass checks
        shell: pwsh
        run: ./tests/Run-CI.ps1

      - name: Build inspectable Biology player uninstaller
        shell: pwsh
        run: ./tools/Build-BiologyUninstaller.ps1 -OutputPath './dist/Uninstall Biology.exe'

      - name: Build redistribution-safe development source package
        shell: pwsh
        run: ./tools/Build-Package.ps1

      - name: Reject forbidden or blocked artifact content
        shell: pwsh
        run: |
          $archives = @(Get-ChildItem -LiteralPath ./dist -Filter '*.zip' -File)
          if ($archives.Count -ne 1) { throw "Expected exactly one development package, found $($archives.Count)." }
          $scanRoot = Join-Path $env:RUNNER_TEMP 'realpass-artifact-scan'
          if (Test-Path -LiteralPath $scanRoot) { Remove-Item -LiteralPath $scanRoot -Recurse -Force }
          Expand-Archive -LiteralPath $archives[0].FullName -DestinationPath $scanRoot
          ./tools/Test-ArtifactPolicy.ps1 -Root $scanRoot

      - name: Upload Biology player uninstaller
        uses: actions/upload-artifact@v4
        with:
          name: biology-player-uninstaller-${{ github.sha }}
          path: dist/Uninstall Biology.exe
          if-no-files-found: error
          retention-days: 14

      - name: Upload development source package
        uses: actions/upload-artifact@v4
        with:
          name: realpass-development-source-${{ github.sha }}
          path: dist/*.zip
          if-no-files-found: error
          retention-days: 14

      - name: Upload offline test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: realpass-offline-reports-${{ github.sha }}
          path: reports/*.json
          if-no-files-found: ignore
          retention-days: 14
